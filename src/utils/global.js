//一些全局的变量
const global = {
	// // 请求地址
	// API_ADDR: "http://10.100.5.199:9000",
	// PROXY_ADDR:"http://10.100.5.105:8093/"

	//打包路由设置，ios/android/h5/default，默认值是default，即pc模式
	runEnviron: 'default',
}

export default global
